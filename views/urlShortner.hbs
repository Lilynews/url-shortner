<div class="row d-flex justify-content-center">
  <div class="card col-6">
    <div class="mt-3">
      <h5 class="d-inline">Shorten link:</h5>
      {{!-- <p>{{host}}/{{randomNumber}}</p> --}}
      <a href="/{{randomNumber}}">{{host}}/{{randomNumber}}</a>
    </div>
    <div class="d-flex justify-content-center my-3">
      <div class="">
        <button onclick="return navigator.clipboard.writeText('{{host}}/{{randomNumber}}')" type="submit" class="btn btn-primary" style="width: 100px;">Copy</button>
      </div>
      <div class="">
        <a class="ms-2 btn btn-secondary" href="/" style="width: 100px;">Back</a>
      </div>
    </div>
  </div>
</div>